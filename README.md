# Docs Agent PR Demo

This repository is a disposable target for testing documentation-agent pull request
creation.

## Expected PR scope

Test changes should normally edit files under `content/docs/**`.

Suggested prompt:

> Update `content/docs/getting-started.md` to add a short troubleshooting section
> about checking environment variables before running the docs service.

